services:
  mineru-api:
    image: mineru-api:pipeline
    build:
      context: .
      dockerfile: mineru/Dockerfile
    container_name: mineru-api-minerU
    environment:
      MINERU_MODEL_SOURCE: local
    volumes:
      - mineru_model_cache_minerU:/root/.cache/modelscope
    networks:
      - internal
    expose:
      - '18080'
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:18080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  backend:
    image: pdf-check-backend:latest
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: pdf-check-minerU
    environment:
      DATA_DIR: /app/runtime
      HF_HOME: /app/.cache/huggingface
      OCR_LANGS: chi_tra+chi_sim+eng
      DEBUG: 'false'
      MINERU_API_URL: 'http://mineru-api-minerU:18080'
    volumes:
      - backend_runtime_minerU:/app/runtime
      - backend_hf_cache_minerU:/app/.cache/huggingface
    networks:
      - internal
    expose:
      - '8000'
    depends_on:
      mineru-api:
        condition: service_healthy
    restart: unless-stopped

volumes:
  backend_runtime_minerU:
  backend_hf_cache_minerU:
  mineru_model_cache_minerU:

networks:
  internal:
    external: true
