# Direct runtime dependencies pinned to the OCI-verified 2026-06-28 baseline.
# Transitive versions are captured in the build SBOM; update these only after the
# six-pair golden benchmark passes on both OCI and the Mac ARM64 reference host.
fastapi==0.138.1
uvicorn==0.49.0
python-multipart==0.0.32
pydantic==2.13.4
pydantic-settings==2.14.2
docling==2.107.0
pandas==3.0.3
openpyxl==3.1.5
pymupdf==1.27.2.3
psutil==7.2.2
imagehash==4.3.2
requests==2.34.2
lxml==6.1.1
# Used directly by services/diff_service.py (pixel diff + pHash). Previously only
# pulled in transitively via imagehash; declared explicitly so a missing scipy can
# never silently make diff_pixels() return [] (image-only PDFs -> 0 diffs).
numpy==2.4.6
scipy==1.17.1
Pillow==12.2.0
