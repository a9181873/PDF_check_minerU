# Critical transitive dependencies observed in the healthy OCI ARM64 containers
# on 2026-06-28. Keep the full resolved inventory in the SPDX SBOM generated by
# build-and-export.ps1; these pins prevent model/parser behavior from drifting.
docling-core==2.85.0
docling-ibm-models==3.13.3
docling-parse==7.0.0
docling-slim==2.107.0
doclang==0.7.0
huggingface-hub==1.21.0
opencv-python==4.13.0.92
pypdfium2==5.10.1
rapidocr==3.9.0
regex==2026.5.9
# CPU-only wheels are installed from the official PyTorch CPU index before the
# remaining requirements. The local version suffix prevents pip from silently
# selecting CUDA 13 wheels on Linux ARM64.
torch==2.12.1+cpu
torchvision==0.27.1+cpu
transformers==5.12.1
tree-sitter==0.25.2
typer==0.24.2
